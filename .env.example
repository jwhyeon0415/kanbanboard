# Supabase — Project Settings → API 에서 복사
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 주의: service_role 키는 절대 여기(클라이언트 노출 env)에 넣지 않는다.
# Google OAuth 클라이언트 ID/Secret 은 앱이 아니라 Supabase 대시보드(Auth → Providers → Google)에 등록한다.
